plugins {
    alias(libs.plugins.quarkus.extension)
}

description = 'Quarkus extension for Pipestream protobuf tools'

quarkusExtension {
    deploymentModule = 'proteus-quarkus-deployment'
}

dependencies {
    implementation platform(libs.quarkus.bom)
    api project(':proteus-mapper-cel')
    api project(':proteus-rest')
    api project(':proteus-server-common')
    api libs.quarkus.arc
}

tasks.matching { it.name == 'validateExtension' }.configureEach {
    enabled = false
}
